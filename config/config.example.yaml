# Copy this file to config/config.yaml and fill in the details.
token: "DISCORD_BOT_TOKEN"
application_id: 123456789012345678
default_timezone: "UTC"
logging:
    level: "INFO"
    logger_channel_id: null
manual_defaults:
    duration_minutes: 1440
scheduling:
    auto_enabled: false
    giveaways: []  # Configure scheduled giveaways here (optional)
    # - id: "daily_gift"
    #   enabled: true
    #   channel_id: 123456789012345678
    #   winners: 1
    #   title: "Daily Giveaway"
    #   description: "Join the daily giveaway!"
    #   start_time: "09:00"  # HH:MM 24-hour format
    #   end_time: "21:00"    # HH:MM 24-hour format
permissions:
    admin_roles: [] # Optional initial role IDs; prefer managing via /giveaway-add-admin-role.
